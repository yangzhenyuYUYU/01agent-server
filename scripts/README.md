# 文件迁移脚本使用说明

## 简介

`migrate_files.py` 是一个用于从其他项目（`D:\project\2025\01editor\01editor-server`）读取文件并复制到当前项目的 `temp/` 目录中的工具脚本。主要用于代码迁移和 AI 分析。

## 功能特性

- ✅ 读取指定文件并打印内容用于校验
- ✅ 自动复制文件到 `temp/01editor-server/` 目录，保持原有目录结构
- ✅ 支持单个文件、多个文件或整个目录的迁移
- ✅ 支持文件扩展名过滤
- ✅ 自动排除 `.git`、`node_modules` 等常见目录
- ✅ `temp/` 目录已配置在 `.gitignore` 中，不会被 Git 跟踪

## 使用方法

### 1. 迁移单个文件

```bash
# 使用相对路径（相对于源项目根目录）
python scripts/migrate_files.py src/main.py

# 使用绝对路径
python scripts/migrate_files.py "D:\project\2025\01editor\01editor-server\src\main.py"
```

### 2. 迁移多个文件

```bash
python scripts/migrate_files.py src/main.py src/config.py src/utils.py
```

### 3. 迁移整个目录

```bash
# 迁移整个 src 目录
python scripts/migrate_files.py --dir src

# 只迁移特定扩展名的文件
python scripts/migrate_files.py --dir src --ext .py .js .ts
```

### 4. 迁移文件但不显示内容

```bash
python scripts/migrate_files.py src/main.py --no-content
```

### 5. 使用批处理文件（Windows）

```bash
scripts\migrate_files.bat src/main.py
```

## 参数说明

- `files`: 要迁移的文件路径（可多个，相对或绝对路径）
- `--dir, -d`: 迁移整个目录
- `--source, -s`: 源项目路径（默认: `D:\project\2025\01editor\01editor-server`）
- `--temp, -t`: 临时目录（默认: `temp`）
- `--no-content`: 不显示文件内容
- `--ext`: 只迁移指定扩展名的文件（用于目录迁移）

## 输出说明

脚本会：
1. 打印文件内容（前 50 行）用于校验
2. 显示文件复制进度
3. 在 `temp/01editor-server/` 目录下创建相同的目录结构
4. 最后显示迁移统计信息

## 示例

### 示例 1: 迁移单个 Python 文件

```bash
python scripts/migrate_files.py src/main.py
```

输出：
```
📦 文件迁移工具
源项目: D:\project\2025\01editor\01editor-server
临时目录: D:\project\2025\gin_web\temp

================================================================================
文件: D:\project\2025\01editor\01editor-server\src\main.py
总行数: 100
================================================================================
   1 | import os
   2 | import sys
   ...
================================================================================

✅ 已复制: src/main.py
   源: D:\project\2025\01editor\01editor-server\src\main.py
   目标: D:\project\2025\gin_web\temp\01editor-server\src\main.py
✅ 文件迁移成功: src/main.py

📊 迁移完成:
   成功: 1 个文件
   失败: 0 个文件
```

### 示例 2: 迁移整个 src 目录

```bash
python scripts/migrate_files.py --dir src
```

### 示例 3: 只迁移 Python 和 JavaScript 文件

```bash
python scripts/migrate_files.py --dir . --ext .py .js .ts
```

## 注意事项

1. 确保已安装 Python 3.x
2. 源项目路径必须存在
3. 文件会保持原有的目录结构
4. `temp/` 目录已配置在 `.gitignore` 中，不会被 Git 跟踪
5. 如果目标文件已存在，会被覆盖

## 目录结构

迁移后的文件会保存在：
```
gin_web/
  temp/
    01editor-server/
      src/
        main.py
        config.py
      ...
```

